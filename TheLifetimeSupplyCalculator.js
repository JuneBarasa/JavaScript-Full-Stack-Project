var currentAge = 21;
var maxAge = 70;
var AmountPerDay = 3;

var total = AmountPerDay * ((maxAge - currentAge) * 365);

console.log("You will need " + total + " to last you until the ripe old age of "+ maxAge)
